name: name of workflow
on : push
jobs: 
   name_of_job:
       runs-on: ubuntu-latest
       steps:
       - name: first step
         uses: actions/checkout@v4

       - name: second step
         run: ls 

       - name: third step
         run: cat README.md
  
  
  
